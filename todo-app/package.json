{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Modern todo list application with local storage",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  }
}
